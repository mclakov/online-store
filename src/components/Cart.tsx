import React from 'react';
import { propsCart } from '../../src/globalTypes';

const Cart = (props: propsCart) => {
    return (
        <div
            className='cart'
        >

        </div>
    );
};

export default Cart;
